<?php

/**
 * إعدادات Geofencing: المسافة المسموحة (بالأمتار) بين موقع الفني وموقع التذكرة
 * عند Check-in و Check-out. تُقرأ من GEOFENCE_MAX_METERS في .env (افتراضي 200).
 */

return [

    /*
    |--------------------------------------------------------------------------
    | أقصى مسافة مسموحة لتسجيل الدخول (Check-in) بالأمتار
    |--------------------------------------------------------------------------
    |
    | يجب أن يكون الفني ضمن هذه المسافة من موقع التذكرة لتسجيل الدخول.
    | للاختبار من الكمبيوتر: ضع رقماً كبيراً (مثلاً 999999) في .env
    |
    */

    'max_meters' => (int) env('GEOFENCE_MAX_METERS', 200),

];
